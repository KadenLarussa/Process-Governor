<UserControl x:Class="ProcessGovernor.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Profiles" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Group rules for gaming, work, streaming, benchmarking, and quiet operation." Style="{StaticResource MutedText}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Profile name" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBox Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,12,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="Add" Style="{StaticResource AccentButton}" Command="{Binding AddProfileCommand}" Margin="0,19,8,0" />
                <Button Grid.Column="2" Content="Activate" Command="{Binding ActivateProfileCommand}" Margin="0,19,8,0" />
                <Button Grid.Column="3" Content="Activate Temp" Command="{Binding ActivateTemporaryCommand}" Margin="0,19,8,0" />
                <Button Grid.Column="4" Content="Clear Active" Command="{Binding ClearActiveProfilesCommand}" Margin="0,19,8,0" />
                <StackPanel Grid.Column="5" Margin="0,0,0,0">
                    <TextBlock Text="Minutes" Foreground="{StaticResource MutedTextBrush}" />
                    <TextBox Text="{Binding TemporaryOverrideMinutes, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Content="Reload" Command="{Binding ReloadCommand}" Margin="0,0,8,0" />
            <Button Content="Delete Selected" Command="{Binding DeleteProfileCommand}" Margin="0,0,8,0" />
            <Button Content="Save Changes" Command="{Binding SaveCommand}" Style="{StaticResource AccentButton}" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Profiles}"
                      SelectedItem="{Binding SelectedProfile}"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      SelectionMode="Single"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Margin="0,0,12,0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Enabled" Width="68">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ToggleButton Style="{StaticResource GridToggleButton}"
                                              IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              Content="{Binding Enabled, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Active" Width="68">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ToggleButton Style="{StaticResource GridToggleButton}"
                                              IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              Content="{Binding IsActive, Converter={StaticResource BoolLabelConverter}, ConverterParameter=Active/Idle}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" />
                    <DataGridTextColumn Header="Auto Process" Binding="{Binding AutoActivateProcessName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120" />
                    <DataGridTextColumn Header="Order" Binding="{Binding PriorityOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="58" />
                    <DataGridTextColumn Header="Rules" Binding="{Binding RuleIds.Count}" IsReadOnly="True" Width="54" />
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="{Binding SelectedProfileName}" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="Pick which saved rules belong to this profile. This replaces editing rule IDs in JSON." Style="{StaticResource MutedText}" />
                    </StackPanel>
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding RuleAssignments}"
                              AutoGenerateColumns="False"
                              IsReadOnly="False"
                              RowHeight="54">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Use" Width="74">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ToggleButton Style="{StaticResource GridToggleButton}"
                                                      IsChecked="{Binding IsAssigned, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Content="{Binding IsAssigned, Converter={StaticResource BoolLabelConverter}, ConverterParameter=In/Out}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Rule" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding RuleName}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Trigger}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
