<UserControl x:Class="ProcessGovernor.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Settings" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Local-only behavior, refresh cadence, tray behavior, notifications, and rollback safety." Style="{StaticResource MutedText}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,14,0">
                            <TextBlock Text="Refresh interval ms" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBox Text="{Binding RefreshIntervalMs, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,14,0">
                            <TextBlock Text="Minimized interval ms" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBox Text="{Binding MinimizedRefreshIntervalMs, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Max log entries" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBox Text="{Binding MaxLogEntries, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,14">
                    <WrapPanel>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding MinimizeToTray}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Minimize to tray" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding MinimizeToTray, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding CloseToTray}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Close to tray" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding CloseToTray, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding StartWithWindows}" Margin="0,0,8,8" ToolTip="Writes or removes the current user Run key entry when settings are saved.">
                            <StackPanel>
                                <TextBlock Text="Start with Windows" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding StartWithWindows, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding CompactMode}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Compact mode" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding CompactMode, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding PauseMonitoringWhenMinimized}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Pause minimized" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding PauseMonitoringWhenMinimized, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding SafeMode}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Safe mode" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding SafeMode, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding EnableNotifications}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Notifications" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding EnableNotifications, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton Style="{StaticResource OptionToggleButton}" IsChecked="{Binding AutomaticRollbackProtection}" Margin="0,0,8,8">
                            <StackPanel>
                                <TextBlock Text="Rollback protection" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding AutomaticRollbackProtection, Converter={StaticResource BoolLabelConverter}, ConverterParameter=On/Off}" FontSize="11" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}" />
                            </StackPanel>
                        </ToggleButton>
                    </WrapPanel>
                </Border>

                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,14,0">
                            <TextBlock Text="Theme" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBox Text="{Binding Theme, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,14,0">
                            <TextBlock Text="Accent color" Foreground="{StaticResource MutedTextBrush}" />
                            <TextBox Text="{Binding AccentColor, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
                        </StackPanel>
                        <Button Grid.Column="2"
                                Content="Save Settings"
                                Style="{StaticResource AccentButton}"
                                Command="{Binding SaveCommand}"
                                HorizontalAlignment="Left"
                                Margin="0,19,0,0" />
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
