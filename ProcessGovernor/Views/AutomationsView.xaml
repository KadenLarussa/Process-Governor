<UserControl x:Class="ProcessGovernor.Views.AutomationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Automations" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Phase 1 rules: process start trigger, priority action, notification, dry run, cooldown, delay, rollback." Style="{StaticResource MutedText}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="170" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Rule name" Foreground="{StaticResource MutedTextBrush}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Process name" Foreground="{StaticResource MutedTextBrush}" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Priority" Foreground="{StaticResource MutedTextBrush}" />
                <TextBlock Grid.Row="0" Grid.Column="3" Text="Cooldown" Foreground="{StaticResource MutedTextBrush}" />
                <TextBlock Grid.Row="0" Grid.Column="4" Text="Delay" Foreground="{StaticResource MutedTextBrush}" />

                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding NewRuleName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,10,0" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewProcessName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,10,0" />
                <ComboBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding AvailablePriorities}" SelectedItem="{Binding NewPriority}" Margin="0,5,10,0" />
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding NewCooldownSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,10,0" />
                <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding NewDelaySeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,10,0" />
                <Button Grid.Row="1" Grid.Column="5" Content="Add Rule" Style="{StaticResource AccentButton}" Command="{Binding AddRuleCommand}" Margin="0,5,0,0" />

                <StackPanel Grid.Row="2" Grid.ColumnSpan="6" Orientation="Horizontal" Margin="0,10,0,0">
                    <CheckBox Content="Send notification" IsChecked="{Binding NewNotificationEnabled}" />
                    <CheckBox Content="Revert on exit" IsChecked="{Binding NewRevertOnExit}" />
                    <CheckBox Content="Dry run" IsChecked="{Binding NewDryRun}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                <Button Content="Reload" Command="{Binding ReloadCommand}" Margin="0,0,8,0" />
                <Button Content="Delete Selected" Command="{Binding DeleteRuleCommand}" Margin="0,0,8,0" />
                <Button Content="Save Changes" Command="{Binding SaveCommand}" Style="{StaticResource AccentButton}" />
            </StackPanel>
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Rules}"
                      SelectedItem="{Binding SelectedRule}"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="220" />
                    <DataGridTextColumn Header="Trigger" Binding="{Binding TriggerSummary}" IsReadOnly="True" Width="240" />
                    <DataGridTextColumn Header="Actions" Binding="{Binding ActionSummary}" IsReadOnly="True" Width="*" />
                    <DataGridCheckBoxColumn Header="Dry Run" Binding="{Binding DryRun, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                    <DataGridCheckBoxColumn Header="Rollback" Binding="{Binding RevertOnExit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
